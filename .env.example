# porta da aplicacao
APP_PORT=8080

# caminho do arquivo do H2, sem a extensao .mv.db.
# relativo ao diretorio de onde a aplicacao for iniciada
APP_DB_PATH=./data/usermanager

# credencial do usuario tecnico que autentica as chamadas da API
APP_ADMIN_USERNAME=admin
APP_ADMIN_PASSWORD=admin123

# perfil ativo do spring
# SPRING_PROFILES_ACTIVE=prod
